# 山十 行先予定表 Ver.5.1.6

## Ver.5.1.6 修正

- レイアウト編集時に背景レイヤーが行先板を覆い、ドラッグハンドルを押すと編集がキャンセルされる問題を修正。
- サイドバーを開いたまま、右側の社員カードを直接クリック・ドラッグできるように変更。
- 背景クリックによる編集キャンセルを廃止。キャンセルは左下の「キャンセル」または右上の×から行います。

Ver.4.7.1までの予定同期・休み掲示板・部署別在席拠点・消しゴム機能を維持し、**端末ごとの行先板レイアウト自由設定**を追加した版です。

## Ver.5.1.3 追加機能

- 表示設定から、行先板に表示する社員を自由にON/OFF
- 社員カードをドラッグ＆ドロップで自由に並び替え
- スマホ等では ↑↓ ボタンでも並び替え可能
- カード列数を「自動 / 1～6列」から指定
- 「自分を左上に固定」をON/OFF可能
- 設定は端末ごとに保存されるため、大型モニター・タブレット・PCごとに別レイアウトを利用可能
- Supabaseへの追加SQLは不要

## Ver.4.7.1 追加機能：全休の期間指定

「全休」だけ、1日ではなく複数日の休暇を1件の予定として登録できるようにしました。

### 予定登録

- 状態が **全休** の場合
  - 開始日と終了日を指定できます
  - 開始・終了時刻の入力は不要です
  - 内部では開始日の 0:00 から終了日の 23:59 までの予定として扱います
- **午前休 / 午後休 / 時間休** は従来どおり1日単位です
- 通常の「外出・会議・現場・出張」等も従来どおり同日内の開始・終了時刻で登録します

### 休み掲示板

表示項目は **日付 / 休みの種類 / 時間 / 氏名** です。

例：

- `9/1（月）～9/3（水） | 全休 | 3日間 | 山田 太郎`
- `9/2（火） | 全休 | 1日 | 田中 一郎`
- `9/4（木） | 午前休 | 午前 | 佐藤 次郎`
- `9/5（金） | 時間休 | 13:00～15:00 | 鈴木 花子`

期間途中の全休予定も、終了日を過ぎるまでは掲示板に表示されます。

## 既存機能

- 複数予定が重なった場合は、現在有効な予定のうち開始時刻が最も新しい予定を優先
- 同じ開始時刻なら後から登録された予定を優先
- 優先予定終了後は、まだ有効な次順位の予定へ自動復帰
- 部署ごとの `default_location` を使った在席復帰
- Supabase Cron + クライアント補助処理による開始・終了処理
- Realtime + 定期再取得による全端末同期
- 毎日0:10（日本時間）の終了済み過去予定自動削除

## Supabase設定

Ver.4.7.1では新しいテーブル列やSQL関数は追加しません。
既存の `schedules.start_at` / `end_at` を複数日にまたがる日時として利用します。

**追加SQLは不要です。**

## GitHubへ更新するファイル

- `app.js`
- `index.html`
- `styles.css`
- `README.md`

`supabase-config.js` は現在のものをそのまま使用してください。


## Ver.4.7.1 追加機能：消しゴム（在席に戻す）
- 行先板の「自分」のカードにだけ「⌫ 在席に戻す」ボタンを表示します。
- 押すと、状態を「在席」、行先を部署マスタの `default_location` に戻します。
- 用件・戻り時間・直行・直帰・メモをクリアし、電話状態を `ok` に戻します。
- 押した時点で実行中の自分の予定がある場合、その予定は `end_done = true` として終了扱いにします。未来の予定は削除・終了しません。
- 大型モニターモードでは誤操作防止のためボタンを非表示にします。


## Ver.4.7.1 修正
- 「⌫ 在席に戻す」は確認ダイアログなしのワンクリック操作に変更。
- 成功・失敗はブラウザのalertではなく画面下部のトーストで通知。
- 在席復帰が成功した後の予定再読込などの補助処理エラーは、復帰失敗として表示しません。
- 手動で状態を「在席」に変更した場合も、部署マスタ `default_location` を必ず使用します。
  - 中津営業所 → 中津事務所
  - セメント部 → セメント事務所
  - その他 → 各部署の設定値（未設定時は本社）


## Ver.5.1.3 サイドバー式レイアウト編集
- 「レイアウト編集」で左側に社員一覧を表示
- 社員をクリックして表示／非表示を切替
- 行先板のカードを直接ドラッグして並び替え
- 列数を編集画面から変更
- 保存内容は従来どおり端末ごとに保持


## Ver.5.1.3
- レイアウト編集のカード並び替えをPointer Events方式へ変更しました。
- マウス・タッチ操作の両方で、左上の☷をつかんで並び替えできます。
- 移動先を緑のガイドで表示し、保存するまで確定しません。


## Ver.5.1.3
- レイアウト編集のドラッグ処理を再構築。ドラッグ中にDOMを動かさず、ドロップ時だけ並び順を確定する方式に変更。
- Pointer Capture中にカードのpointer-eventsを無効化しないよう修正。Edge/Chromeでのドラッグキャンセルを防止。


## Ver.5.1.6
- レイアウト編集のメンバー選択サイドバーでは、ログイン中の本人を常に一番上に表示します。
- 本人以外の社員は従来の登録順を維持します。
- 本人には「自分」バッジを表示します。
- 右側の行先板レイアウト・保存済みの並び順には影響しません。


## Ver.5.1.6 変更点
- ログイン中の本人を行先板の左上（1番目）に常時固定します。
- レイアウト編集でも本人のカードはドラッグ移動できません。
- 本人のカードは表示から外せません。
- サイドバーでも本人は先頭表示のままです。
- 2番目以降の社員はこれまでどおり自由に並び替えできます。
- 端末ごとに保存済みのレイアウトがあっても、本人だけは必ず左上へ補正されます。


## Ver.5.1.6 薄緑・アイコン版
- サイト全体を薄緑ベースに変更
- 山十シンボルを薄く背景にした「山十 行先表」アイコンを追加
- favicon / Apple Touch Icon 対応
- Ver.5.1.6 の既存機能は維持
